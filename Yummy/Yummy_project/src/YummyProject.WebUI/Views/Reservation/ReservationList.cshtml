@model List<GetReservationDTO>
@{
    ViewData["Title"] = "ReservationList";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    int count = 0;
}


<section class="section">
    <div class="section-body">
        <div class="row">
            <div class="col-12 col-md-12 col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h4>Reservation</h4>
                    </div>
                    <div class="card-body">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th scope="col">#</th>
                                    <th scope="col">Name and Surname</th>
                                    <th scope="col">Reservation Date</th>
                                    <th scope="col">Reservation Time</th>
                                    <th scope="col">Delete</th>
                                    <th scope="col">Update</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    count++;
                                    <tr>
                                        <th scope="row">@count</th>
                                        <td>@item.NameSurname</td>
                                        <td>@item.ReservationDate</td>
                                        <td>@item.ReservationTime</td>
                                        <td><a href="/Reservation/DeleteReservation/@item.ReservationId" class="btn btn-danger">Delete</a></td>
                                        <td><a href="/Reservation/UpdateReservation/@item.ReservationId" class="btn btn-success">Update</a></td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                        <a href="/Reservation/CreateReservation/" class="btn btn-info">New Reservation</a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

